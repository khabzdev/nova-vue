{
  "name": "nova-vue",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "playground": "turbo run dev --filter=@nova-vue/playground",
    "lint": "oxlint --ignore-path .gitignore .",
    "lint:fix": "oxlint --fix --ignore-path .gitignore .",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "oxlint": "^0.15.0",
    "turbo": "^2.8.3",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@10.5.2"
}
