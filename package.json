{
  "name": "lattice",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "bun run --filter '@lattice/ui' build",
    "dev": "bun run --filter '@lattice/ui' dev",
    "playground": "bun run --filter '@lattice/playground' dev",
    "lint": "oxlint --ignore-path .gitignore .",
    "lint:fix": "oxlint --fix --ignore-path .gitignore .",
    "type-check": "bun run --filter '@lattice/ui' type-check"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxlint": "^0.15.0",
    "typescript": "^5.7.0"
  }
}
